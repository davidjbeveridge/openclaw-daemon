# openclaw-daemon

A lightweight macOS daemon that runs `openclaw --fix` every hour.

## Install

```bash
./install.sh
```

## Uninstall

```bash
./uninstall.sh
```

## Logs

```bash
tail -f daemon.log
```
